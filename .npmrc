# Privy's transitive `permissionless` declares a peerOptional ox@^0.8 that skews against
# viem's bundled ox@0.14. The peer is optional (account-abstraction path, not used in swap MVP),
# so we accept npm's resolution rather than pinning viem back.
legacy-peer-deps=true
